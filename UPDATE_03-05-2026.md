<div dir=rtl>

# סיכום שינויים — תמיכה ב-PYANNOTE לדיאריזציה בעברית

מסמך זה מסכם את השינויים שבוצעו בשלושת הקבצים כדי להפעיל דיאריזציה של PYANNOTE
עם גרסת `pyannote.audio 4.x` ו-`torch 2.7.1`, אחרי שגרסאות חדשות שברו את הקוד המקורי.

---

## רקע הבעיה

המעבר לדיאריזציה של PYANNOTE (במקום מנוע ה-clustering של IVRIT) נכשל בשרשרת של שגיאות,
כולן נובעות מאי-התאמה בין הקוד המקורי של `ivrit` לבין גרסת `pyannote.audio 4.x` החדשה:

1. `Pipeline.from_pretrained() got an unexpected keyword argument 'use_auth_token'` — הפרמטר הוחלף ב-`token`.
2. `'DiarizeOutput' object has no attribute 'itertracks'` — ב-4.x הפלט עטוף ב-`DiarizeOutput`, וה-`Annotation` נמצא תחת `.speaker_diarization`.
3. `name 'np' is not defined` — `numpy` לא יובא בתוך המתודה `_match_speaker_to_interval`.

ההחלטה הסופית: לבצע fork ל-`ivrit-py`, לתקן בו את הבאגים, ולהתקין מה-fork ב-Docker image.

---

## 1. `diarization.py`

**מיקום:** ב-fork `nitzano707/ivrit-py` → הנתיב `ivrit/diarization.py`
(מותקן ל-image דרך `pip install "ivrit[all] @ git+https://github.com/nitzano707/ivrit-py.git"`)

**שלושה תיקונים, כולם במחלקה `PyannoteDiarizationEngine`:**

1. **תאימות טוקן** — במתודה `diarize`, הקריאה ל-`Pipeline.from_pretrained`:
   - לפני: `Pipeline.from_pretrained(checkpoint_path, use_auth_token=use_auth_token)`
   - אחרי: `Pipeline.from_pretrained(checkpoint_path, token=use_auth_token)`

2. **תאימות `DiarizeOutput`** — במתודה `diarize`, לפני בניית ה-DataFrame, חולץ ה-`Annotation` מהפלט החדש:
   ```python
   annotation = diarization.speaker_diarization if hasattr(diarization, 'speaker_diarization') else diarization
   diarization_df = pd.DataFrame(
       annotation.itertracks(yield_label=True),
       columns=["segment", "label", "speaker"],
   )
   ```
   (השורה תומכת גם ב-4.x החדש וגם בגרסאות ישנות, בזכות `hasattr`.)

3. **ייבוא numpy חסר** — במתודה `_match_speaker_to_interval` נוספה השורה `import numpy as np`
   מיד אחרי ה-docstring (לפני חישוב ה-intersection/union), כי המתודה השתמשה ב-`np`
   בלי לייבא אותו ברמת המתודה.

---

## 2. `infer.py`

**מיקום:** ב-repo `nitzano707/runpod-serverless-ENGLISH-HEBREW` → `infer.py`
(נכנס ל-image דרך `ADD infer.py .`)

**שינוי אחד:** בבלוק `diarize_only` (דיאריזציה בלבד, ללא תמלול), נוספה שורת ה-`annotation`
לפני הלולאה על `itertracks`, באותה לוגיקה כמו ב-`diarization.py`:
```python
annotation = diarization.speaker_diarization if hasattr(diarization, 'speaker_diarization') else diarization

for turn, _, speaker in annotation.itertracks(yield_label=True):
    ...
```

**הערה:** במהלך הפיתוח נוסף זמנית פאץ' של `monkey-patch` ל-`Pipeline.from_pretrained` בראש הקובץ
כפתרון מהיר. לאחר שהבאגים תוקנו ב-fork של `diarization.py`, הפאץ' הזמני הוסר וה-`import ivrit`
הוחזר לראש הקובץ כרגיל.

---

## 3. `Dockerfile`

**מיקום:** ב-repo `nitzano707/runpod-serverless-ENGLISH-HEBREW` → `Dockerfile`

**שלושה שינויים לעומת הגרסה המקורית:**

1. **התקנת `git`** — נדרש כדי להתקין חבילה ישירות מ-GitHub:
   - לפני: `RUN apt install -y ffmpeg`
   - אחרי: `RUN apt install -y ffmpeg git`

2. **משתנה סביבה `PACKAGE_VERSION`** — נוספה השורה `ENV PACKAGE_VERSION=0.2.5` לפני התקנת ivrit.
   ה-build של החבילה (hatchling) דורש את המשתנה הזה כדי לקבוע את גרסת החבילה,
   אחרת ההתקנה נכשלת ב-`environment variable 'PACKAGE_VERSION' is not set`.

3. **התקנה מה-fork במקום מ-PyPI** — שימוש בתחביר Direct URL התקני (`name[extra] @ URL`):
   - לפני: `RUN pip3 install ivrit[all]==0.2.5 huggingface-hub==0.36.0 runpod`
   - אחרי: `RUN pip3 install "ivrit[all] @ git+https://github.com/nitzano707/ivrit-py.git" huggingface-hub==0.36.0 runpod`

   חשוב: ה-`[all]` הכרחי — הוא מביא את התלויות האופציונליות (`faster-whisper`, `pyannote-audio`,
   `speechbrain`, `scikit-learn` וכו'). בלעדיו, ה-build נכשל ב-`No module named 'faster_whisper'`.

---

## הערה לגבי `app.py` (לא ברשימת שלושת הקבצים)

בנפרד משלושת הקבצים לעיל, זוהתה בעיית דיוק בתצוגת הדוברים שמקורה ב-`app.py`
(הפונקציה `normalize_runpod_output`): היא לקחה דובר אחד ברמת המקטע והתעלמה מתיוג
הדוברים המדויק שכבר קיים ברמת המילה (`word.speaker`). התיקון המוצע — פיצול מקטע
לפי רצפי דובר ברמת המילה — היה בתהליך יישום דרך Cursor ואינו חלק משלושת הקבצים שעודכנו ב-RunPod.
